{
  "name": "apishift-monorepo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Enterprise API Auto-Migration Engine & GitHub Bot (Deterministic AST Engine with Hybrid AI Fallback)",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "dev": "pnpm -r --parallel dev",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.24.0",
    "@typescript-eslint/parser": "^8.24.0",
    "eslint": "^10.8.0",
    "eslint-config-prettier": "^10.0.1",
    "eslint-plugin-prettier": "^5.2.3",
    "prettier": "^3.5.1",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "engines": {
    "node": ">=20.0.0 || >=24.0.0"
  }
}
