name = "apishift-bot"
main = "src/index.ts"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[vars]
ENVIRONMENT = "production"
DEFAULT_BRANCH = "main"

# Note: Environment secrets must be configured via wrangler secret:
# - WEBHOOK_SECRET
# - GITHUB_TOKEN
